@model List<Skill>

<table class="table table-hover">
	<thead>
		<tr>
			<th>#</th>
			<th>Name</th>
			<th>Edit</th>
			<th>Delete</th>
		</tr>
	</thead>
	<tbody>
		@foreach (var item in Model)
		{
			<tr>
				<td>@item.Id</td>
				<td>@item.Name</td>
				<td><a href="@Url.Action("EditSkill", "Home", new { id = item.Id })">Edit</a></td>
				<td><a href="@Url.Action("DeleteSkill", "Home", new { id = item.Id })">Delete</a></td>
			</tr>
		}
	</tbody>
</table>

<a href="@Url.Action("EditSkill", "Home")" class="btn btn-primary">Add Skill</a>
